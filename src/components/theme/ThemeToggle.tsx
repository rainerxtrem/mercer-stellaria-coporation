/**
 * Composant supprimé : l'application est verrouillée en thème sombre.
 * Export conservé pour éviter les erreurs d'import résiduelles.
 */
export function ThemeToggle(_: { className?: string }) {
  return null;
}
